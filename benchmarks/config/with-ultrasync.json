{
  "mcpServers": {
    "ultrasync": {
      "command": "uv",
      "args": ["run", "--directory", "${ULTRASYNC_DIR}", "ultrasync", "mcp"],
      "env": {
        "ULTRASYNC_PROJECT_ROOT": "${ULTRASYNC_PROJECT_ROOT}"
      }
    }
  }
}
