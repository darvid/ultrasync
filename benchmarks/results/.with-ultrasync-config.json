{
  "mcpServers": {
    "ultrasync": {
      "command": "uv",
      "args": ["run", "--directory", "/home/david/dev/ultrasync", "ultrasync"],
      "env": {
        "ULTRASYNC_PROJECT_ROOT": "${PROJECT_ROOT}"
      }
    }
  }
}
